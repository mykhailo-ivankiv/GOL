const  render = (board) => "";
const nextStep = (board) => {
    const newBoard = []
    return newBoard;
}

const getNeibours = (board, [x, y]) => {
    return 1;
}

const getNextCellState = (board, [x, y]) => {
    return 0 || 1;
}
